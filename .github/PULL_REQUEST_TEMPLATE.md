## Description

<!-- Describe the changes in this PR. -->

## Related issue(s)

<!-- Link to the issue(s) this PR addresses, e.g. "Fixes #123". -->

## Checklist

- [ ] Tests added or updated; `uv run pytest` passes.
- [ ] Lint and format: `uv run ruff check src tests scripts` and `uv run ruff format src tests scripts` (or `uv run pre-commit run --all-files`).
- [ ] Scope matches the issue; no unrelated changes.
